name: Keep live site awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Wake Render
        run: curl -fsS --max-time 90 https://linkboostgrowth.site/api/health
